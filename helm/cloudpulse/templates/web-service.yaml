apiVersion: v1
kind: Service
metadata:
  name: cloudpulse-web
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: cloudpulse-web
  ports:
    - port: 3000
      targetPort: 3000
  type: ClusterIP
